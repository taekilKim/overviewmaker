# Overviewmaker Frontend (Next.js + shadcn UI)

## Run

npm install
npm run dev

## Deploy (Vercel)

- Root Directory: `frontend`
- Build Command: `npm run build`
- Output: default (Next.js)
- Env: `NEXT_PUBLIC_API_BASE=https://overviewmaker.onrender.com`
